<h1>Current Categories</h1>
<table class="table table-hover">
  <tr>
    <th>Category Name</th>
    <th>Actions</th>
  </tr>
<% @categories.each do |q| %>
  <tr>
    <td><%= q.name %></td><td><%= link_to "edit", edit_category_path(q) %></td>
  </tr>

<% end %>
</table>
<%= paginate @categories %>
